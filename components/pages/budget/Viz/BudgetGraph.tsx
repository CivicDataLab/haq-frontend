import React from 'react';
import styled from 'styled-components';

const BudgetGraph = () => {
  return <Wrapper>Bar Graph</Wrapper>;
};

export default BudgetGraph;

export const Wrapper = styled.section``;
